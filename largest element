#include<iostream>
#include<climits>
using namespace std;
int main(){
    int n;
    n=5;
    int a[n]={8,4,2,1,7};
    int max=INT_MIN;
    for(int i=0;i<n;i++){
        if(a[i]>max){
            max=a[i];
           
        }
    }
    cout<<max;
    return 0;
}
